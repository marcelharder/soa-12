import { AppRoutingModule } from "./app-routing.module";
import { HomeComponent } from "./home/home.component";



describe('Root path', () => {
    let component: AppRoutingModule;
    // test that the root path resolves to the HomeComponent
    /* it('should resolve to the HomeComponent', () => {
      expect(AppRoutingModule.routes[0]).toEqual({
          path: '',
          component: HomeComponent
      });
    }); */
  });
  

  